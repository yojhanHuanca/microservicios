server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bd_categoria_dev
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

empresa:
  ambiente: desarrollo
