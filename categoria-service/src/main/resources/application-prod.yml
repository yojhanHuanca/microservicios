server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://servidor/bd_categoria
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

empresa:
  ambiente: produccion
