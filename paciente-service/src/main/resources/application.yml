server:
  port: 8085

spring:
  application:
    name: paciente-service
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/bd_paciente?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
